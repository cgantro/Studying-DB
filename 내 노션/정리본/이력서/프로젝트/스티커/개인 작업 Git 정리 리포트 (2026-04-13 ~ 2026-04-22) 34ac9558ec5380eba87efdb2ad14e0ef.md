# 개인 작업 Git 정리 리포트 (2026-04-13 ~ 2026-04-22)

## **1) 기간/대상**

- 기간: `2026-04-13 00:00` ~ `2026-04-22` (로컬 저장소 기준)
- 집계 대상 작성자: `홍윤표`, `yoonpyo`
- 집계 키: 동일 이메일 `cgantro@gmail.com`
- 집계 방식: `git log --since="2026-04-13 00:00" --author="cgantro@gmail.com"`

## **2) 기술별 작업 요약**

### **A. GitLab CI/CD (파이프라인 분리/오케스트레이션)**

- 루트 파이프라인에서 이벤트별 라우팅(`mr_build_test`, `mr_ai_review`, `push_deploy`) 트리거 구성.
- MR AI 리뷰 파이프라인에서 `mr_metrics` 선행 후 도메인(Frontend/Backend/AI) child pipeline 트리거 구조로 구성.
- 공통 변경 감지를 위한 `common_trigger_changes` 앵커를 도입/확장하여 도메인 파이프라인 재실행 조건 정리.
- `mr_metrics` 아티팩트(dotenv) 전달을 통한 도메인 파이프라인 변수 전달 경로를 안정화.

근거

- `.gitlab-ci.yml`: `workflow`, `trigger_mr_build_test`, `trigger_mr_ai_review`, `trigger_push_deploy` (1, 14, 29, 44행)
- `.gitlab/ci/mr-ai-review.yml`: `mr_metrics`, `frontend_domain_pipeline`, `backend_domain_pipeline`, `ai_domain_pipeline` (45, 63, 96, 128행)

### **B. 코드리뷰 자동화(LLM) 및 안정화**

- 모델 라우팅을 단일 모델 기본(`gpt-5-mini`)으로 정리하고, 실행 모델 표기를 코멘트 렌더링에 반영.
- GMS OpenAI endpoint 경유 호출과 provider 호출 실패/재시도/타임아웃 처리 로직을 강화.
- `IncompleteRead`, HTTP 재시도, 네트워크 타임아웃 재시도 경로를 코드에서 명시적으로 처리.
- Frontend 설치 경로에서 `node_modules`/캐시 기반 최적화 및 실패시 복구 경로를 적용.

근거

- `.gitlab/scripts/review_modules/scoring.py`: `CODE_REVIEW_MODEL` 기본값, 모델 라우팅 (72~73행)
- `.gitlab/scripts/post_mr_comment.py`: `OPENAI_TIMEOUT_SEC`, `RETRY_COUNT`, `GMS_KEY`, provider 실행 (32, 34, 71, 175~195행)
- `.gitlab/scripts/review_modules/providers.py`: `IncompleteRead`/timeout/retry 처리, GMS endpoint (133~137, 155, 166, 177, 203~211, 358행)
- `.gitlab/scripts/frontend_review.sh`: node_modules 캐시 hit 및 npm install 재시도 경로 (61, 73, 75~77행)

### **C. Jira/커밋 컨벤션 자동화**

- 브랜치명 형식(`type/scope-task-JIRA-KEY`) 및 커밋 제목 형식(`[JIRA-KEY] Type(scope): summary`)을 규약화.
- `commit-msg` 훅에서 Jira 키 존재/일치 검사와 자동 정규화(NORMALIZED_HEADER) 적용.
- MR 템플릿에 Jira 키, 브랜치/제목 컨벤션, Jira 연결 체크리스트를 반영.

근거

- `CONVENTIONS.md`: Jira 연동 컨벤션 및 훅 동작 설명 (1, 73, 86, 129~133행)
- `.githooks/commit-msg`: `BRANCH_PATTERN`, `STRICT_PATTERN`, Jira 키 검사, 자동 정규화 (11~12, 29, 43~44, 105행)
- `.gitlab/merge_request_templates/Default.md`: Jira 체크리스트 (3, 30~32행)

### **D. Backend 기능/기반 작업**

- 이메일 인증 회원가입 API 및 Gmail 메일 연동(`S14P31A408-54`) 구현 및 관련 정책 문서화.
- 개발/운영 DB 프로파일 분리(`S14P31A408-31`)와 SpringBoot 프로젝트 베이스 정리.
- 엔티티 초안 작성 및 dev H2-console 접근 완화(`S14P31A408-3`) 반영.
- Java 주석 표준화/AGENTS 규칙 정비 커밋 포함.

주요 근거 파일(커밋 기반 상위 변경)

- `S14P31A408-54`: `Server/src/main/java/.../SecurityConfig.java`, `.../infra/mail/GmailApiProperties.java` 등
- `S14P31A408-31`: `Server/src/main/resources/application-dev.yml`, `application-prod.yml`, `application.yml`
- `S14P31A408-3`: `Server/src/main/java/com/project/server/domain/...` 엔티티 다수

## **3) 이슈별 주요 결과**

- `S14P31A408-64` (38커밋): MR AI 코드리뷰 파이프라인 구축/분리, 모델 단일화, 타임아웃/재시도/파서 안정화, metrics artifact 전달 안정화.
- `S14P31A408-54` (4커밋): 이메일 인증 회원가입 + Gmail 연동, 계정 통합 정책 문서화, Java 주석 규칙 정비.
- `S14P31A408-31` (4커밋): 개발/운영 DB 프로파일 분리, SpringBoot 기반 세팅 정리.
- `S14P31A408-3` (4커밋): 엔티티 초안 작성, dev h2-console 접근 권한 완화.
- `S14P31A408-37` (2커밋): Git/GitLab/Jira 컨벤션 문서화 및 훅 적용.
- `S14P31A408-48` (2커밋): 브랜치 네이밍 컨벤션 수정.

반복 안정화로 묶은 구간

- 동일 이슈 `S14P31A408-64`에서 `CI yml 문법`, `mr_metrics entrypoint/artifact`, `모델/타임아웃/재시도`, `merge/fix`가 연속적으로 반복되며 안정화 진행.

## **4) 정량 지표**

### **A. 총량**

- 총 커밋 수: `66`
- 타입 분포(정규 패턴 기준):
    - `Feat`: 18
    - `Chore`: 14
    - `Fix`: 13
    - `Test`: 8
    - `Docs`: 1
    - `Other`(Resolve/Delete/Main/hook-test 등): 12

### **B. 일자 분포**

- `2026-04-15`: 7
- `2026-04-20`: 9
- `2026-04-21`: 17
- `2026-04-22`: 33

### **C. 이슈 분포(커밋 메시지 내 Jira 키 기준)**

- `S14P31A408-64`: 38
- `S14P31A408-31`: 4
- `S14P31A408-3`: 4
- `S14P31A408-54`: 4
- `S14P31A408-37`: 2
- `S14P31A408-48`: 2

### **D. 상위 변경 영역(파일 최상위 경로 기준)**

- `.gitlab`: 122
- `Server`: 120
- `.gitlab-ci.yml`: 11
- `FashionApp`: 11
- `ai`: 10

## **5) 리스크/미확인 항목**

- 사실: 저장소 내 CI 구성/스크립트 변경은 다수 확인됨.
- 해석: "GitLab Runner 직접 설정 변경"(예: runner 등록/태그/executor 수정)은 저장소 파일 근거가 약함.
    - 본 리포트에서는 Runner 관련을 "잡 구조/캐시/아티팩트/트리거 최적화" 수준으로 한정함.
- 사실: 점수/게이트 규칙은 저장소 코드에서 확인됨.
- 해석: 요청하신 "스토리 포인트 규칙"은 Jira 필드 자체 변경이 아닌, 리뷰 스코어링/게이트 자동화 규칙으로 해석함.

---

## **재현 검증 로그**

### **검증 1: 커밋 수/일자 분포 재현**

- 명령

```bash
git log --since="2026-04-13 00:00" --author="cgantro@gmail.com" --pretty=format:"%h|%ad|%an|%s" --date=short
git log --since="2026-04-13 00:00" --author="cgantro@gmail.com" --date=short --pretty=format:"%ad" | Group-Object
```

- 결과: 총 66커밋, 날짜 분포 `7/9/17/33` 일치.

### **검증 2: 이슈 키 집계 ↔ 기술 요약 대조**

- 명령

```bash
git log --since="2026-04-13 00:00" --author="cgantro@gmail.com" --pretty=format:"%s"
```

- 결과: `64/-54/-31/-3/-37/-48`가 기술 요약( CI/CD, 리뷰자동화, Jira 컨벤션, Backend 기능 )에 모두 매핑됨.

### **검증 3: Runner 직접 설정 여부 분리**

- 명령

```bash
rg -n "runner|Runner|gitlab-runner|executor|tag" -S .
```

- 결과: Runner 등록/운영 설정 파일 근거는 없음. 리포트에서 추정 항목으로 분리 표기함.

지난주(2026-04-20 ~ 2026-04-26) 작업을 git author=yoonpyo 기준으로 정리하면 아래입니다.

- 총 커밋: 69개
- 집중 이슈:
1. S14P31A408-64 (34건): GitLab CI/AI 코드리뷰 파이프라인 구축·분리·안정화, 모델/타임아웃/캐시 최적화, mr_metrics 및 오류 복구
2. S14P31A408-82 (8건): 인프라/배포 개선 (push-deploy 분리, Redis 도입, Traefik Docker API 버전 이슈 수정, 자동배포 체계 정비)
3. S14P31A408-54/55/80/87/89/92/98: 백엔드 인증·회원가입/로그인, 응답 포맷 문서화, 테스트 재구성, 운영 로그/헬스체크/로그백 정리, 탈퇴 토큰 폐기 리팩터링
    - 도메인별 핵심:
4. Common/CI: AI 리뷰 파이프라인 초안 → 분리 → 안정화까지 연속 개선
5. Backend: 엔티티 초안, 이메일 인증 회원가입 API+Gmail 연동, h2-console 권한 조정, 탈퇴 시 RefreshToken 일괄 폐기 전환
6. Infra/Deploy: Redis 기반 저장소 전환, 서버 배포 파이프라인 이관, 운영 로그/헬스체크 설정 통합