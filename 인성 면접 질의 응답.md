- 자기소개: **성능 병목 분석 + 협업 기준 정리**
- 지원동기: **SDS 설명 줄이고 내 경험 연결**
- 강점: **문서화가 아니라 기준화/불일치 방지**
- 어려움 극복: **측정 방식과 개선 구조 중심**
- 갈등: **성격 갈등보다 기준 불일치 해결**
- 압박: **혼자 해결이 아니라 역할 재조정**
- 실수: **AI 리뷰는 대체가 아니라 보조 검증 장치**

### 1. 1분 자기소개

저는 프로젝트를 진행하며 두 가지 역량을 길러왔습니다.  

첫 번째는 **병목을 분석하고 구조적으로 개선하는 역량**입니다. RobotPal 프로젝트에서는 카메라 스트리밍 과정에서 렌더링과 인코딩 처리가 맞물리며 프레임 드랍이 발생하는 문제를 확인했고, 이를 생산자-소비자 구조로 분리해 처리 흐름을 개선한 경험이 있습니다.

두 번째는 **팀 기준을 문서+자동화로 고정하는 경험**입니다. Sticker에서 브랜치/커밋/MR에 Jira 키를 남기는 컨벤션을 문서로 정리했고(`CONVENTIONS.md`), `commit-msg` 훅으로 형식 위반을 커밋 단계에서 막았습니다. 백엔드는 `ApiResponse(code/message/data)` 표준 응답과 예외 매핑을 맞춘 뒤 테스트 기반으로 API 문서를 생성했습니다. 최근에는 `AGENTS.md`에 모노레포 경계와 금지사항을 적어 AI 도구도 같은 기준으로 작업하게 했습니다.

이러한 경험을 바탕으로 삼성SDS에서도 안정적이고 운영 가능한 소프트웨어를 만드는 데 기여하겠습니다..

### 2. 지원동기
키워드 : 실시간 데이터 처리, 상태 불일치 방지, 협업 기준 정리(특히 중요)
경험 : 팀 문서 협업 (좀 더 구체적으로)
제품군들을 보ㅇ

저는 프로젝트를 진행하며 실시간 데이터 처리, 상태 불일치 방지, 협업 기준 정리의 중요성을 경험했습니다. 특히 API 명세, Jira 기준, 커밋·브랜치 규칙을 정리하면서 협업의 핵심은 단순한 소통이 아니라, 팀이 같은 기준과 맥락으로 일할 수 있는 구조를 만드는 것이라고 느꼈습니다.

이후 삼성SDS의 Brity 제품군을 보며, 기업의 업무 흐름을 연결하고 반복 업무와 정보 불일치를 줄이는 시스템에 관심을 갖게 되었습니다. 저도 삼성SDS에서 기업 고객의 업무가 더 안정적이고 효율적으로 이어질 수 있도록, 협업과 업무 흐름을 시스템적으로 개선하는 개발자로 성장하고 싶습니다.

### 3. 장/단점
### 직무적 장점

**병목을 감으로 판단하지 않고, 구조와 흐름을 나눠서 분석하는 점**

> 제 직무적 장점은 문제를 감으로 해결하기보다, 데이터 흐름과 처리 구조를 나눠 원인을 찾는 점입니다.  
> RobotPal 프로젝트에서 카메라 스트리밍 중 프레임 드랍이 발생했을 때, 단순히 성능이 부족하다고 보기보다 렌더링, 인코딩, 큐 적재 흐름을 나눠 확인했습니다. 그 결과 렌더링과 인코딩이 같은 흐름에서 맞물리는 것이 병목이라는 점을 확인했고, 생산자-소비자 구조로 분리해 개선했습니다.  
> 저는 이런 식으로 문제를 구조적으로 쪼개고, 병목 지점을 찾아 개선하는 데 강점이 있습니다.

핵심 키워드:

- 병목 분석
- 데이터 흐름 분리
- 구조적 개선
- 측정 기반 개선

---

### 직무적 단점

제 직무적 단점은 문제를 처음 마주했을 때 구조와 기준을 먼저 명확히 잡으려는 성향이 있어, 초반 실행 속도가 늦어질 때가 있다는 점입니다.

예전에는 팀 프로젝트 초반에 Git 브랜치 규칙, Jira 상태 기준, API 명세 작성 방식, 커밋 규칙 등을 한 번에 정리하려다 보니, 실제 기능 구현보다 기준 정리에 시간이 먼저 쓰이는 경우가 있었습니다. 기준을 잡는 것 자체는 필요했지만, 모든 규칙을 처음부터 완성하려 하면 개발 속도가 늦어질 수 있다는 점을 느꼈습니다.

그래서 최근에는 방식을 바꿔, 처음부터 모든 기준을 만들기보다 **바로 충돌이 생길 수 있는 최소 기준만 먼저 정하고 구현을 진행**하고 있습니다. 예를 들어 API 연동이 필요한 기능이라면 응답 필드, 에러 형식, 인증 여부처럼 프론트와 백엔드가 바로 맞춰야 하는 부분만 먼저 고정하고, 나머지 규칙은 개발하면서 보완하는 식입니다.

AI Agent를 활용할 때도 마찬가지로, 처음부터 모든 자동화 규칙을 만들기보다 파일 위치, 코드 스타일, 수정 금지 범위, 테스트 실행 기준처럼 결과물에 바로 영향을 주는 항목부터 AGENTS.md에 정리하고 있습니다.

이 단점이 좋은 이유:

- 실제 단점처럼 들림
- 개발자 성향과 자연스럽게 연결됨
- 보완 방식이 구체적임
- SDS에서 “느린 사람”으로 보이지 않게 방어 가능

---

### 인성적 장점

고통에 내성이 강하다.

제 강점은 **무던하게 버티고 끝까지 해내는 태도**입니다.

군 복무 중 허리를 다쳐 전역하게 된 경험이 있습니다. 당시에는 걷는 것도 쉽지 않았고, 꾸준히 해오던 운동도 제대로 할 수 없어 많이 힘들었습니다.

하지만 상황을 바꿀 수 없다면, 지금 할 수 있는 것부터 하자는 생각으로 재활과 근력 보강을 꾸준히 이어갔습니다. 그 결과 현재는 많이 회복된 상태이고, 이 경험을 통해 어려운 상황에서도 감정에만 머무르지 않고 할 일을 찾아 지속하는 태도를 갖게 됐습니다.

삼성SDS에서도 처음 마주하는 업무나 어려운 상황이 있더라도 조급해하기보다, 필요한 것을 차근차근 배우고 끝까지 책임지는 구성원이 되겠습니다.


---

### 인성적 단점

제 약점은 문제 상황에서 상대의 감정보다 해결 방법을 먼저 생각하는 편이라는 점입니다.

해결도 중요하지만 팀으로서 상대의 감정 또한 고려하는 것도 팀의 효율에 큰 영향을 끼친다는 것을 알게되었습니다.

밴드마스터로 활동할 때 합주나 곡 방향이 맞지 않으면 빠르게 고치고 싶어서 “이건 이렇게 해야 한다”는 식으로 바로 피드백을 준 적이 있습니다. 제 의도는 팀 결과물을 좋게 만들기 위한 것이었지만, 나이 차이가 있는 후배들에게는 그 방식이 다소 직설적이고 어렵게 느껴질 수 있다는 말을 들었습니다.

이후에는 바로 해결책을 말하기보다 먼저 상대가 어떤 의도로 연주했는지, 어떤 부분이 어려웠는지 듣고 나서 방향을 제안하려고 노력했습니다. 이 경험을 통해 협업에서는 맞는 답을 말하는 것만큼, 상대가 받아들일 수 있는 방식으로 전달하는 것도 중요하다고 느꼈습니다.

### 4. 어려움을 극복한 경험

C++에서는 멀티스레딩 기반으로 처리하던 구조가 웹 빌드 후에는 기대한 대로 동작하지 않았고, 카메라 프레임 처리 과정에서 프레임 드랍이 발생했습니다. 원인을 확인해보니 웹에서는 Emscripten pthread를 사용하더라도 SharedArrayBuffer를 쓰기 위한 브라우저 보안 조건, 즉 COOP와 COEP 설정이 필요했습니다.

그래서 빌드 옵션에서 pthread를 활성화하고, 정적 배포 환경에서도 해당 조건이 맞도록 설정을 보완했습니다. 이후 웹에서도 Web Worker 기반 멀티스레딩이 정상 동작하는 것을 확인했습니다.

이 경험을 통해 성능 문제를 해결할 때는 코드 구조뿐 아니라 빌드 옵션, 브라우저 보안 정책, 배포 환경까지 함께 봐야 한다는 점을 배웠습니다.

---

# 꼬리질문 대비

#### Q1. 웹은 싱글스레드 아닌가요?

> JavaScript에서 DOM을 직접 다루는 흐름은 메인 스레드 중심입니다. 하지만 Web Worker를 사용하면 DOM 접근이 필요 없는 계산, 인코딩, 데이터 처리 같은 작업은 별도 실행 흐름으로 분리할 수 있습니다. Emscripten pthread는 이 Web Worker를 활용해 C++의 pthread 모델을 웹 환경에서 동작하도록 매핑합니다.

#### Q2. Emscripten pthread를 쓰려면 뭘 설정해야 하나요?

> 컴파일과 링크 단계에 `-pthread` 옵션을 적용해야 합니다. 필요하면 `-sPTHREAD_POOL_SIZE`로 워커 풀 크기도 지정할 수 있습니다. 단, 브라우저에서 실제로 동작하려면 SharedArrayBuffer 사용 조건도 맞아야 합니다.

#### Q3. COOP, COEP가 뭔가요?

> SharedArrayBuffer는 보안상 아무 페이지에서나 사용할 수 없고, 페이지가 cross-origin isolated 상태여야 사용할 수 있습니다. 이를 위해 COOP와 COEP 응답 헤더가 필요합니다. 쉽게 말하면 브라우저가 “이 페이지는 안전하게 메모리를 공유해도 된다”고 판단하게 만드는 조건입니다.

#### Q4. 왜 GitHub Actions에서 문제가 됐나요?

> 정확히는 GitHub Actions의 문제가 아니라, 정적 배포나 테스트 서버 환경에서 COOP/COEP 응답 헤더를 직접 제어하기 어려웠던 문제였습니다. 빌드는 성공해도 브라우저 실행 시 보안 조건이 맞지 않으면 pthread가 정상 동작하지 않았습니다.

#### Q5. JS 코드를 넣었다는 게 무슨 뜻인가요?

> 서버에서 헤더를 직접 넣는 게 정석이지만, 당시 정적 배포 환경에서는 그게 어려웠습니다. 그래서 서비스워커 기반 스크립트를 빌드 산출물에 포함해 필요한 보안 조건을 보완했고, 이후 브라우저에서 멀티스레딩이 정상 동작하는 것을 확인했습니다.

#### Q6. 운영 환경이라면 어떻게 할 건가요?

> 운영 환경이라면 서비스워커 방식보다는 Nginx, Spring 서버, CDN 설정 등에서 COOP/COEP 응답 헤더를 명시적으로 내려주는 방식이 더 적절하다고 생각합니다. 당시에는 제한된 정적 배포 환경에서 문제를 해결하기 위한 방식이었습니다.

#### Q7. 이게 프레임 드랍 개선과 어떻게 연결되나요?

> 카메라 프레임 처리에서 인코딩 같은 무거운 작업이 메인 흐름에 영향을 주면 프레임 드랍이 발생할 수 있습니다. 그래서 Web Worker 기반으로 무거운 작업을 분리해 메인 처리 흐름의 부담을 줄이는 방향으로 개선했습니다.
### 5. 팀에서의 역할

팀에서는 주로 작업 기준과 개발 흐름을 맞추는 역할을 했습니다.
Jira에서는 Epic을 큰 기능 단위로 두고, 실제 구현은 Task로 나눴습니다. 각 Task에는 피보나치 점수를 부여하고, Epic에는 하위 Task 점수가 자동 집계되도록 해서 스프린트 작업량을 팀이 같이 볼 수 있게 했습니다.

또한 Git 브랜치와 커밋 규칙, 변경사항 공유 방식을 맞춰 어떤 작업이 어떤 이슈와 연결되는지 추적 가능하게 했습니다. 최근에는 AI Agent 활용 기준도 문서화해서, 생성된 코드가 팀의 구조와 컨벤션을 벗어나지 않도록 관리했습니다.

저는 팀에서 앞에서 지시하기보다는, 기준을 명확히 해서 협업 비용을 줄이는 역할을 주로 맡았습니다.

#### Q1. 왜 Epic에는 점수를 안줬는가?
> Epic은 기능 묶음 -> 추정단위가 너무 큼
> 실제 구현 및 리뷰 단위는 Task -> Task에만 점수
### 6. 갈등 해소 경험

### 7. 압박 상황해소

### 8. 실수 대응

### 9. 리더십 스타일

1. 팀의 기준/방향성을 맞춤
2. 밴드 경험을 통해 부드러운 소통을 배울 수 있었음

### 10. 최근에 관심있게 보고있는 내용
1. 자소서 -> AI 거버넌스
2. AI를 휘둘러야 한다(하네스 엔지니어링)

### 11. 실패 경험

### 12. 동료들이 본인을 어떻게 평가하나?

### 13. 장기적 목표

### 14. 시간 관리

### 15. 스트레스 관리

	
