# C++17 핵심

## 주요 기능
- structured binding
- if constexpr
- std::optional / std::variant / std::string_view
- parallel STL(환경 의존)

## 실무 포인트
- optional/variant로 에러/상태 표현을 명시적으로 만들 수 있다.
- string_view는 복사 비용을 줄이지만 수명 관리가 중요하다.
- if constexpr은 템플릿 분기 가독성을 크게 개선한다.
