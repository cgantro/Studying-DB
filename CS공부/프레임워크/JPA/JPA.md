# JPA

## 핵심
- ORM으로 객체와 테이블 매핑
- 영속성 컨텍스트(1차 캐시)와 변경 감지(dirty checking) 기반 동작

## 실무 이슈
- N+1 쿼리
- 지연 로딩 초기화 시점
- flush 타이밍 오해로 인한 성능 저하

## 운영 팁
- fetch join/EntityGraph로 조회 경로 명시
- 트랜잭션 경계 안에서 도메인 로직 수행
- SQL 로그/실행 계획으로 검증
