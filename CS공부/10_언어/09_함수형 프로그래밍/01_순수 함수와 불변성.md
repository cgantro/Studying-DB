# 순수 함수와 불변성

## 순수 함수
- 같은 입력이면 항상 같은 출력
- 외부 상태 변경(side effect) 없음

## 불변성
- 상태를 변경하지 않고 새 값을 생성
- 동시성 버그와 추론 비용을 줄인다.

## 실무 효과
- 테스트 단순화
- 캐시 키 안정화
- 병렬 처리 시 안정성 향상

## 주의점
- 무분별한 복사로 메모리 비용 증가 가능
- 구조적 공유/적절한 데이터 구조 선택 필요
