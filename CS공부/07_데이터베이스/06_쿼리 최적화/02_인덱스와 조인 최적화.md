# 인덱스와 조인 최적화

## 인덱스
- WHERE/JOIN/ORDER BY 패턴 기준으로 설계
- 복합 인덱스는 컬럼 순서가 핵심

## 조인 최적화
- 불필요한 컬럼/행을 먼저 줄인 뒤 조인
- 조인 키 타입/정렬 상태를 맞춰 비용 절감

## 안티패턴
- SELECT * 남발
- 함수 적용으로 인덱스 무력화
- 카디널리티 낮은 컬럼 단독 인덱스 과다 생성
