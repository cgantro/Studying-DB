# 1NF와 원자값

## 1NF 정의
- 각 컬럼 값은 더 이상 분해되지 않는 원자값이어야 한다.
- 한 셀에 복수 값(예: "a,b,c")을 저장하지 않는다.

## 왜 중요한가
- 검색/정렬/인덱싱이 단순해진다.
- 데이터 무결성과 업데이트 일관성이 좋아진다.

## 위반 예시
- phone 컬럼에 여러 번호를 CSV로 저장

## 개선
- 별도 테이블(1:N) 분리
- 외래키로 관계 명시
